spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR}
        namespace: ${NACOS_NAMESPACE_DEV:dev}
        group: DEFAULT_GROUP
      config:
        server-addr: ${NACOS_ADDR}
        namespace: ${NACOS_NAMESPACE_DEV:dev}
        group: DEFAULT_GROUP
        file-extension: yaml

